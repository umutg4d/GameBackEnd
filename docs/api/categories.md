## POST /open-category

It fetches the user_id and category_id from the request parameters and inserts a new row for the UserCategory table using these parameters.

## GET /get-user-categories
It fetches the user with the user_id parameter from the request parameters and then fetch all the relevant categories user unlocked.